{
  "name": "subpanel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "worker:dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
